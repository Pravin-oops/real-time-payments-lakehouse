name: lakehouse_gold
version: "1.0.0"
config-version: 2
profile: lakehouse
model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  lakehouse_gold:
    silver:
      schema: silver
      materialized: view
    gold:
      schema: gold
      materialized: table
      tags: ["gold"]